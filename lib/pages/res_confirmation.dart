import 'package:flutter/material.dart';

class ResConfirmationPage extends StatelessWidget {
  const ResConfirmationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Res Confirmation Page'),
      ),
    );
  }
}
