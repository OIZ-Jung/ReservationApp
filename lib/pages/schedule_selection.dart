import 'package:flutter/material.dart';

class ScheduleSelectionPage extends StatelessWidget {
  const ScheduleSelectionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Schedule Selection Page'),
      ),
    );
  }
}
